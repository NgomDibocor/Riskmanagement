<aura:component controller="OrmAssessmentController">
    <aura:registerEvent name="DisplayAssessment"
		type="c:OrmDisplayAssessmentEvt"/>
    <aura:handler action="{!c.doInit}" name="init" value="{!this}" />
    <aura:attribute name="assessments" type="Assessment__c[]" description="" />
	<!--<p>Liste des Assessment de l'utilisateur</p>-->
	
    <div class="slds-grid" style="margin-bottom:30px">
        <div class="slds-col slds-size_12-of-12">
        </div>
		 <lightning:button label="New Assessment" 
                          variant="brand"
                          class="slds-button"
                          onclick="{!c.newAssessment}"/>        
    </div>
    
    <!--<div class="slds-grid">
        <aura:iteration items="{!v.assessments}" var="item" indexVar="i">           
                <div class="slds-col slds-size_1-of-1">
                    <article class="slds-card">
                        <div class="slds-card__header slds-grid">
                            <header class="slds-media slds-media_center slds-has-flexi-truncate">
                                <div class="slds-media__figure">
                                    <span class="slds-icon_container slds-icon-standard-account" title="{!item.Name}">                
                                        <span class="slds-assistive-text">{!item.Name}</span>
                                    </span>
                                </div>
                                <div class="slds-media__body">
                                    <h2 class="slds-card__header-title">
                                        <a href="javascript:void(0);" class="slds-card__header-link slds-truncate" title="Accounts">
                                            <span class="slds-text-heading_small">{!item.Name}</span>
                                        </a>
                                    </h2>
                                </div>
                                <div class="slds-no-flex">
                                    <button class="slds-button slds-button_neutral">New</button>
                                </div>
                            </header>
                        </div>
                        <div class="slds-card__body slds-card__body_inner">{!item.orm_description__c}</div>
                            <footer class="slds-card__footer">
                                <a class="slds-card__footer-action" href="javascript:void(0);">{!item.orm_description__c}
                                    <span class="slds-assistive-text">Accounts</span>
                                </a>
                            </footer>
                    </article>
                </div>            
        </aura:iteration>
    </div>-->
    
</aura:component>